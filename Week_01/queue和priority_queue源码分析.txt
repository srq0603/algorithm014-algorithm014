Queue是队列，是先进先出的数据结构，add和offer方法都是从队列头部添加一个元素，区别在于add方法在容器满时，会抛出异常；remove和poll方法都是用于从头部移出一个元素，区别在于队列为空时，remove方法会抛出异常。
priority queue在queue的基础上增加了增加了优先级的判断，使用了最小堆的结构，其元素的顺序是按照自然顺序或者按照它们构建的时候所采用的构造器。且不允许有null元素，优先级队列可以通过poll, remove, peek, 和element这些方法来获取队列的头。